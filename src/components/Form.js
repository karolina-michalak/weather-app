import React from 'react';

const Form = props => {
    return (
        <div>
            <input type="text" placeholder="city"/>
            <button>search</button>
        </div>
    )
}
export default Form;
